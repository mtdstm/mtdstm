Drop the hero background loop here as `hero-loop.mp4` (and ideally a
`hero-loop-mobile.mp4` swapped in via a media query / JS check for
smaller screens — see src/components/molecules/VideoBackground.tsx).

Recommended: 10-20s loop, muted-safe audio track, H.264 mp4, under 6MB,
1920x1080 (desktop) / 720x1280 (mobile).
